const greeting = "HELLO CHILDREN OF GOD";
export default greeting;

export default function snooperizer(name) { //function named snooperizer THAT WILL TAKE IN A NAME.
    console.log(`${name}izzle`); //calls function with an inserted name with 'izzle' on the end.
}

//you can EXPORT IMPORT a number of different code blocks using export default function
export const API_KEY = "KNWERTHERWOITY4234";

export function reverseName(name) {
    console.log(name.toUpperCase())
}

export const powers = {
    jump: 10,
    punch: 20,
    swim: 50
};
